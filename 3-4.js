
// 3-4 편지 💌

// 또쓱; 앗 근데 효자였음

// 한 글자 크기 가로 2cm , message 최소가로길이 return
//message length * 2cm 만큼 편지지 필요해

function solution (message){
  return  message.length * 2;
}

const solution = (message) => message.length * 2;


// 기택
function solution (message){
  return  message.length * 2;
}

//민희
function solution(message) {
  var answer = message;

  var num = answer.length*2;

  console.log(num);
  return num;
}

//민규
function solution(message) {
  return message.length * 2
}
solution("happy birthday!")